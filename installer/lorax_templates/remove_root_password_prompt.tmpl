## Remove the root password prompt from the installer
<%
  try:
    # Check if root (uid 0) shell is /bin/bash
    import os
    from pyanaconda.addons import AddonSection

    # Append a new section
    pass
  except Exception as e:
    # Safely handle any exceptions
    pass
%>

%post
# Skip root password prompt during installation
# This allows automated installs to proceed
%end
